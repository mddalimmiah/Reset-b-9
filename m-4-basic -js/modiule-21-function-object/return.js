function tenTimes(number){
    const result=number *10;
    return result;
}

function cutHalf(number){
    const res=number / 2;
    return res;
}

const output=tenTimes(50);
console.log("output:", output);

const halfResult=cutHalf(500);

console.log("cuthalf:", halfResult)